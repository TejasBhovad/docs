---
title: "Simple GUI using Tkinter"
sidebar_position: 5
description: "Write a Python program that uses the Tkinter library to create a window with a title."
---

import Heading from "../../../../components/Heading";

# <Heading name="banner" description="Simple GUI">Python</Heading>

> Write a Python program that uses the Tkinter library to create a window with a title.

```python
# title and window tkinter
from tkinter import Tk

root = Tk()
root.title("Custom window title")
root.mainloop()

```

:::note

To compile and run the program, you can use the following commands:

```bash
python3 foo.py
```

:::
