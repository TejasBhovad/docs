---
title: "pallindrome string"
sidebar_position: 11
description: "Implement a Python function that checks if a given string is a palindrome"
---

import Heading from "../../../../components/Heading";

# <Heading name="banner" description="pallindrome string">Python</Heading>

> Implement a Python function that checks if a given string is a palindrome

```python
def is_palindrome(string):
    return string == string[::-1]

print(is_palindrome('racecar')) # True
print(is_palindrome('hello')) # False
# Output:
    # True
    # False

```

:::note

To compile and run the program, you can use the following commands:

```bash
python3 foo.py
```

:::
