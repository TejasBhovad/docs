---
title: "Sum and Average"
sidebar_position: 9
description: "Calculate sum and average of list"
---

import Heading from "../../../../components/Heading";

# <Heading name="banner" description="Sum and Average">Python</Heading>

> Calculate sum and average of list

```python
# calculate sum and average of list

list = [1,2,3,4,5]

sum = 0
for i in list:
    sum = sum + i
avg = sum/len(list)
print("Sum: ", sum)
print("Average: ", avg)

```

:::note

To compile and run the program, you can use the following commands:

```bash
python3 foo.py
```

:::
